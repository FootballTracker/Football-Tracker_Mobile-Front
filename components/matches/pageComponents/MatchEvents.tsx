import { memo } from "react";
import { ThemedText } from "@/components/DefaultComponents/ThemedText";

function MatchEvents() {
    return (
        <ThemedText>Eventos</ThemedText>
    )
}

export default memo(MatchEvents);